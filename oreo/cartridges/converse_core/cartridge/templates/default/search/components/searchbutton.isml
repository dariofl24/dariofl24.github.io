<isif condition="${!pdict.CurrentHttpParameterMap.isCheckout.booleanValue}">
    <iscomment>This includes global utilities into the pages</iscomment>
    <isinclude template="util/modules"/>
    <isinclude template="util/adaptiveImageModules.isml">
    <iscomment>
        This creates the search button in the top header
        this will launch the search dialog
    </iscomment>
    <div id="search-button">
        ${Resource.msg('global.search','locale',null)}
    </div>
</isif>
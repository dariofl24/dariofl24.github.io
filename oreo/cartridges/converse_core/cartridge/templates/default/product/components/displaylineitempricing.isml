<iscontent type="text/html" charset="UTF-8" compact="true"/>
<iscomment>
    Renders product line item pricing information.
    p_productli : the product line item
 </iscomment>

<isset name="ProductLineItem" value="${pdict.p_productli}" scope="page"/>
<isset name="LineItemAdjustedPrice" value="${ProductLineItem.getAdjustedPrice()}" scope="page"/>

<span class="price-sales"><isprint value="${LineItemAdjustedPrice}"/></span>

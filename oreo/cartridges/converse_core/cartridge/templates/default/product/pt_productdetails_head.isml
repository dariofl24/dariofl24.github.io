<isscript>
    var pid;
    if (pdict.Product.custom.productType.value === 'dyo') {
        pid = pdict.Product.ID;
    } else {
        pid = pdict.Product.variationModel.master.ID;
    }
</isscript>

<link rel="canonical" href="${URLUtils.abs('Product-Show','pid', pid)}"/>

/**
 *   @output From : String
 *   @output Template : String
 *
 */
importPackage( dw.system );
importPackage( dw.web );

importScript("converse_core:constants.ds");

function execute( args : PipelineDictionary ) : Number
{

    args.From = Resource.msg('mail.noreply.address', 'mail', null);;
    args.Template =Resource.msg('order.confirmation.template', 'mail', null);

    return PIPELET_NEXT;
}

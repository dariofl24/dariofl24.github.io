importScript("converse_core:constants.ds");
importScript("converse_core:common/libNamespace.ds");

importScript("int_genesco:objects/AbstractShipmentResponse.ds");

(function() {

    var GO_NS = integration.genesco.objects;

    var RegularShipmentResponse = GO_NS.AbstractShipmentResponse.extend({
                 
    });
    
    Namespace.extend(GO_NS, { RegularShipmentResponse : RegularShipmentResponse });
    
})();

/**
*   @output PaymentMethodId : String
*/

importScript("converse_core:constants.ds");

function execute( args : PipelineDictionary ) : Number {
    
    args.PaymentMethodId = converse.constants.PaymentMethodId.IDeal;
    
   return PIPELET_NEXT;
}

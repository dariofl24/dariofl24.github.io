/**
 * Run all suites
 *
 * @output Unit: Object
 */
importPackage(dw.system);

importScript("module_test:Framework/Unit.ds");

function execute(args : PipelineDictionary) : Number {
    args.Unit = new Unit(Unit.RunSuites);
    return PIPELET_NEXT;
}
